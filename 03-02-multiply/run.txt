# SOLVE SAMPLE

(cat data/sample.161.txt | perl tools/convert.pl > src/sample.s) && gcc -g src/sample.s -o run && ./run

# SOLVE SAMPLE2

(cat data/sample2.48.txt | perl tools/convert.pl > src/sample2.s) && gcc -g src/sample2.s -o run && ./run

# SOLVE PUZZLE

(cat data/puzzle.txt | perl tools/convert.pl > src/puzzle.s) && gcc -g src/puzzle.s -o run && ./run